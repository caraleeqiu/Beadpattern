import React from "react";
import ReactDOM from "react-dom/client";
import BeadMaker from "./BeadMaker.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BeadMaker />
  </React.StrictMode>
);
